# s0lac3

A tiny black-and-white friend chat prototype with:

- Simple username/password login screen
- One-room local chat UI
- Space for sharing game links
- Dot particle background animation

## Run locally

Open `index.html` in a browser.

For a local server:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.
